<%- model_class = Tour -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human %></h1>
</div>

<div id="tour-info" class="row-fluid">
  <div class="span5">
    <%= gmaps(
          :map_options => { :container_class => "map_container", :id => "map_canvas", :provider_key => ENV["GOOGLE_KEY"],
          :center_latitude => @current_location['lat'], :center_longitude => @current_location['lng'] },
          :markers => { :data => @markers, :options => { :rich_marker => true } },
          :polylines => { :data => @polylines }
    ) %>
    <%= link_to t('.edit', :default => t("helpers.links.edit")), edit_tour_path(@tour), :class => 'btn' %>
  </div>
  <div class="span7">
    <div class="pull-right span4 well">
      <p><%= @tour.start_date %></p>
      <p><%= @tour.end_date %></p>
      <p>
      <%= (@tour.end_date - @tour.start_date).to_i %> days
      </p>
      <p><%= number_to_currency(@tour.price) %></p>
    </div>
    <p><%= @tour.title %></p>
    <p><%= @tour.description %></p>
    <p>Itinerary</p>
    <% @locations.each do |location| %>
      <p>
        <span class="lead"><%= location.title %></span><br />
        <%= location.description %>
      </p>
    <% end %>
  </div>
</div>

<!-- <div class="form-actions">
  <%#= link_to t('.back', :default => t("helpers.links.back")),
              tours_path, :class => 'btn'  %>
  <%#= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_tour_path(@tour), :class => 'btn' %>
  <%#= link_to t('.destroy', :default => t("helpers.links.destroy")),
              tour_path(@tour),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
 -->